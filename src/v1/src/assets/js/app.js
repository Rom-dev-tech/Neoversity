// const refs = {
//   links: document.querySelectorAll("[data-link]"),
//   urlParams: new URLSearchParams(window.location.search),
// };

// console.log(refs.urlParams);

// refs.links.forEach((link) => {
//   link.href = `${link.href}&${refs.urlParams}`;
// });
